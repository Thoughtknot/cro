fn main() int {
    if true {
        var k := 3 + 3
    } else {
        var i := 1
    }
    var a := call()
    var d := new typea("foo", 1)
    loop d.r < 50 {
        system.print(d.r)
        a = new typea("a", d.r)
        d.r = d.r + 1
    }
    d.s = "noo"
    return 0
}

obj typea {
    string s
    int r
}

obj typec {
    string[] s
    int r
}

fn call() int {
    var r := 3
    loop r < 6 {
        r = r + 1
    }
    return r
}
